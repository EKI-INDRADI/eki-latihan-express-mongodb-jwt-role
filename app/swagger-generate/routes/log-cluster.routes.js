const log_cluster_create = require('../controllers/log-cluster/create.controllers');

module.exports = {
    // paths: {
        '/api/log-cluster/create': {
            ...log_cluster_create
        }
    // }
}